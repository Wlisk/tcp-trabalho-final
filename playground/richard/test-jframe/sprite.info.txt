Sprite(image_path)
+ getCurrentIMG(): pega a imagem do sprite atual
+ setSpriteType(type_sprite): muda o sprite para o que se quer
+ config(img_dimension)
+ addAction(actiona_name, action_type, pos_start, img_quantity)
+ update(): update the sprites

ups vs fps

ups: takes care of logic - player move, events, etc.
- same on all pcs

fps: draws the game scene - the level player, enemies, etc.
- can be changed
